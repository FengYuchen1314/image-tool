services:
  image-tool:
    image: fengyuchen1314/image-tool:latest
    container_name: image-tool
    ports:
      - "30050:30050"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
